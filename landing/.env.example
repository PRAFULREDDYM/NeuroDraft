# Copy this file to `.env.local` in this directory (`landing/.env.local`).
# Never commit `.env.local` — it is gitignored.

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_LANDING_URL=http://localhost:3001
